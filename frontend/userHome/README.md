
  # R-bites Food Ordering App

  This is a code bundle for R-bites Food Ordering App. The original project is available at https://www.figma.com/design/TUynN7EHx97nTnCXAp8SUw/R-bites-Food-Ordering-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  